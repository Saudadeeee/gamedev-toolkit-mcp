# Aseprite MCP — Python dependencies
# For development/reference. Use `uv sync` for reproducible installs.
#
# Install (recommended):
#   uv sync
#
# Install (pip fallback):
#   pip install -r requirements.txt

# Core MCP framework
mcp[cli]>=2.0.0

# .env loading for ASEPRITE_PATH / GODOT_PATH
python-dotenv>=1.0.0

# Async runtime for the stdio entrypoint
anyio>=4.0.0

# --- Dev dependencies (optional) ---
# pytest>=8.0.0
# black>=24.0.0
# flake8>=7.0.0
